// @golias/ui — biblioteca de componentes React compartilhados entre os
// aplicativos desktop (escritório) e web (campo/fiscal).
//
// Ainda vazio nesta primeira etapa: o pacote está apenas montado para que,
// a partir da Fase 3 (captura de RDO em campo), os componentes visuais
// comuns (formulários, tabelas, cartões de status, etc.) possam ser
// extraídos para cá e reutilizados pelos dois front-ends.
export {};
